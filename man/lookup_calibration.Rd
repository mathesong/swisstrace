% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup_calibration.R
\name{lookup_calibration}
\alias{lookup_calibration}
\title{Look up the calibration factor for a study by date}
\usage{
lookup_calibration(
  cal_dates,
  cal_values,
  filename = NULL,
  date = NULL,
  method = c("last", "nearest", "exact"),
  max_gap = NULL,
  tz = "UTC"
)
}
\arguments{
\item{cal_dates}{Vector of calibration dates (Date or coercible).}

\item{cal_values}{Vector of recorded calibration factors (same length as \code{cal_dates}).}

\item{filename}{Path(s) to an existing raw \code{.crv} file; the acquisition start
date is read from each. Mutually exclusive with \code{date}. Vectorised.}

\item{date}{The acquisition date(s), given directly. Accepts a \code{Date}/\code{POSIXct},
a date string ("YYYY-MM-DD" or "YYYY/MM/DD"), or a \code{swisstrace_correct()} result
list (its \verb{$date} is used). Mutually exclusive with \code{filename}. Vectorised.}

\item{method}{How to match: "last" (most recent calibration on or before the study
date; default), "nearest" (closest in either direction), or "exact".}

\item{max_gap}{Optional maximum allowed gap in days between the study and the
matched calibration; matches beyond it are returned as \code{NA} with a warning.}

\item{tz}{Timezone for reading dates from \code{.crv} files (default "UTC").}
}
\value{
A tibble with one row per study: \code{study_date}, \code{calibration_factor},
\code{cal_date} (the calibration used), and \code{gap_days} (study_date - cal_date;
positive = calibration predates the study).
}
\description{
Identify the study either by \code{filename} (a raw \code{.crv} whose acquisition date is
read from the file) or by \code{date} (given directly). Supply exactly one of the two.
}
