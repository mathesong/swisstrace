% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swisstrace_convert_batch.R
\name{swisstrace_convert_batch}
\alias{swisstrace_convert_batch}
\title{Batch-convert twilite .crv files listed in a manifest}
\usage{
swisstrace_convert_batch(
  manifest,
  output_folder = NULL,
  cal_dates = NULL,
  cal_values = NULL,
  bids_dir = NULL,
  cal_method = c("last", "nearest", "exact"),
  cal_max_gap = NULL,
  ...
)
}
\arguments{
\item{manifest}{Path to an \code{.xlsx}/\code{.xls}/\code{.csv}/\code{.tsv} file, or a data.frame, with
one row per recording. Recognised columns (case-insensitive): \code{filename}
(required), \code{isotope} (required unless \code{half_life} is given), \code{pet_start}
(optional - \code{"HH:MM:SS"} or seconds; blank auto-detects), \code{calibration_factor}
(optional - looked up when absent/blank), \code{sub}/\code{ses} (optional BIDS labels), and
\code{half_life} (optional, seconds). Relative \code{filename}s are resolved against the
manifest's folder.}

\item{output_folder}{Folder for the outputs (and the calibration log). Defaults to
the manifest's folder, or the working directory if \code{manifest} is a data.frame.}

\item{cal_dates, cal_values}{Your record of calibrations (dates and factors), used to
fill in \code{calibration_factor} for any row that lacks one, via \code{\link[=lookup_calibration]{lookup_calibration()}}.
Required only if some rows need a lookup.}

\item{bids_dir}{Optional BIDS dataset root passed through to \code{\link[=swisstrace_process]{swisstrace_process()}}
(used for rows that carry a \code{sub}).}

\item{cal_method, cal_max_gap}{Passed to \code{\link[=lookup_calibration]{lookup_calibration()}} (\code{method}, \code{max_gap}).}

\item{...}{Further arguments forwarded to \code{\link[=swisstrace_process]{swisstrace_process()}} for every row
(e.g. \code{recording}, \code{overwrite}, \code{lead}, \code{frame_scheme}).}
}
\value{
(invisibly) a tibble with one row per manifest entry: \code{filename}, \code{isotope},
\code{calibration_factor}, \code{status} ("ok"/"error"), and \code{message} (the error, if any).
}
\description{
Reads a manifest spreadsheet describing many recordings and runs
\code{\link[=swisstrace_process]{swisstrace_process()}} on each. See the columns below.
}
