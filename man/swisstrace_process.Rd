% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swisstrace_process.R
\name{swisstrace_process}
\alias{swisstrace_process}
\title{Correct a twilite .crv and write its outputs (corrected crv, QC plot, BIDS)}
\usage{
swisstrace_process(
  filename,
  calibration_factor,
  isotope,
  ...,
  output_folder = NULL,
  sub = NULL,
  ses = NULL,
  bids_dir = NULL,
  recording = "autosampler",
  overwrite = TRUE
)
}
\arguments{
\item{filename}{Path to the raw twilite \code{.crv} to correct.}

\item{calibration_factor}{Per-study calibration multiplier (counts/sec -> kBq/cc).}

\item{isotope}{Isotope name for the decay correction (REQUIRED — not stored in the
\code{.crv}). E.g. "F18". Pass \code{half_life} via \code{...} instead for an isotope not in the
built-in table.}

\item{...}{Further correction arguments passed to \code{\link[=swisstrace_correct]{swisstrace_correct()}}
(e.g. \code{pet_start}, \code{lead}, \code{half_life}, \code{frame_scheme}).}

\item{output_folder}{Root for outputs. Default: the folder containing \code{filename}.
Outputs are written to subfolders within it.}

\item{sub, ses}{Optional BIDS subject/session labels (e.g. "01", "01"). If \code{sub} is
given, BIDS PET blood files are written (under \code{output_folder/BIDS/...} by default,
or into \code{bids_dir} if supplied). Supplying \code{ses} without \code{sub} is an error. With
neither, no BIDS output is made.}

\item{bids_dir}{Optional path to an existing BIDS dataset root. When given, the BIDS
blood files are placed there instead of under \code{output_folder}: if a
\verb{sub-<sub>[_ses-<ses>]..._pet.nii[.gz]} image is found within it, the blood files
are written into that image's folder and named from its entities (so the recording
is associated with that scan); otherwise they go into
\verb{bids_dir/sub-<sub>[/ses-<ses>]/pet/}. Requires \code{sub}.}

\item{recording}{BIDS \verb{recording-} label for the blood file (default "autosampler").}

\item{overwrite}{Overwrite existing files (default TRUE).}
}
\value{
(invisibly) a named list of the paths written.
}
\description{
Correct a twilite .crv and write its outputs (corrected crv, QC plot, BIDS)
}
