% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swisstrace_correct.R
\name{swisstrace_correct}
\alias{swisstrace_correct}
\title{Correct and calibrate a twilite .crv file}
\usage{
swisstrace_correct(
  file,
  calibration_factor,
  isotope,
  pet_start = NULL,
  half_life = NULL,
  lead = 20,
  frame_scheme = data.frame(width = c(1, 10), end = c(180, 600)),
  zero_first_frame = TRUE,
  baseline_k = 3,
  min_run = 5,
  baseline_init = 10,
  baseline_min = 20,
  tz = "UTC",
  ...
)
}
\arguments{
\item{file}{Path to a raw twilite .crv file.}

\item{calibration_factor}{Per-study calibration multiplier (counts/sec -> kBq/cc).}

\item{isotope}{Isotope name for the half-life lookup. REQUIRED: it is not stored
in the \code{.crv}, and a wrong isotope silently corrupts the decay correction.
E.g. "F18", "C11", "O15", "Ga68". Needed unless \code{half_life} is supplied directly.}

\item{pet_start}{PET scan start ("time 0") - the last of the main inputs, alongside
\code{file}, \code{calibration_factor} and \code{isotope}. Usually worth supplying: it sets the
reference for the decay correction (the effect is small for long-lived isotopes
such as F-18 but grows for shorter half-lives). Accepts a clock time as a string
\code{"HH:MM:SS"} (e.g. \code{"11:16:30"}, taken on the recording's own date) - the typical
way the scan start is noted - or a POSIXct, or a number of seconds after the start
of the twilite recording. If NULL (the default), time 0 is estimated from the
curve (see \code{lead}).}

\item{half_life}{Optional half-life in seconds, used in place of the \code{isotope}
lookup (for isotopes not in the built-in table).}

\item{lead}{Seconds to place time 0 before the detected rise onset (only used
when \code{pet_start} is NULL). Default 20, following swisstrace's guidance to start
the twilite >=20 s before injection. Automatically reduced if fewer than \code{lead}
seconds were recorded before the onset (it keeps whatever is available). The
lead actually applied is returned as \verb{$lead}.}

\item{frame_scheme}{data.frame with columns \code{width} and \code{end} (seconds) defining
the output resampling. Default = 1 s frames to 180 s, then 10 s frames to 600 s.}

\item{zero_first_frame}{If TRUE (default), the first output frame
is set to exactly 0.}

\item{baseline_k}{Detection threshold, in robust SDs above the background mean. Default is 3.}

\item{min_run}{Number of consecutive samples that must exceed the threshold to
mark the rise onset (guards against isolated noise spikes). Default 5.}

\item{baseline_init}{Seconds at the start of the recording used to seed the
baseline median + MAD before forward accumulation (auto-detect mode). Default 10.}

\item{baseline_min}{Warn if the rise is detected with less than this many seconds
of accumulated baseline (i.e. the twilite was started too late). Default 20.}

\item{tz}{Timezone used to interpret the recorded timestamps (default "UTC";
only affects the printed clock time, not any computed quantity).}

\item{...}{Reserved for future options; currently ignored.}
}
\value{
A list with:
\item{tac}{tibble: frame \code{time} (mid-time, s), \code{activity} (kBq/cc), and
\code{frame_start}, \code{frame_end}, \code{frame_dur}.}
\item{calibration_factor, background, isotope, half_life, lambda}{correction
parameters used.}
\item{date, start_time, acq_start, acq_end, acq_duration}{acquisition timing.}
\item{pet_start, t0_seconds, t0_detected, lead}{the time-0 used and how.}
\item{n_raw, n_background, file, frame_scheme, raw}{provenance, plus the raw
samples as a tibble (\code{time}, \code{coincidence}, \code{singles1}, \code{singles2}).}
}
\description{
Correct and calibrate a twilite .crv file
}
